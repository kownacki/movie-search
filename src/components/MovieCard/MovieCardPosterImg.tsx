import styled from 'styled-components';

// Standard movie poster aspect ratio is 27x40 or 0.675
const posterWidth = '200px';

const MovieCardPosterImg = styled.img`
  width: ${posterWidth};
`;

export default MovieCardPosterImg;
